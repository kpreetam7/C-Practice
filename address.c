#include <stdio.h>
#include <stdlib.h>
#define pi 3.14
int main() {
     int x=5 ;
      y=3;
    y = pi; ///calculate the rhs and atore it in the left side

    printf("x=%d\n",x);
    printf("y=%lf\n\n",y);

    ///address of the veriable
    printf("the address of y in memory is:%p",&y);

    ///use "&" to address the variable


    return 0;
}