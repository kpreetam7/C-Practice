#include<conio.h>
#include<stdio.h>
void main()
{
    int n;
    printf("Enter any year:");
    scanf("%d", &n);
    if(n%4==0)
    printf("year is a leap year");
    else
    
    printf("year is not a leap year");  /* code */
    
    getch ();;

}